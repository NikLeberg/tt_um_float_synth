# SPDX-License-Identifier: MIT

# Load VHDL design into Yosys.
ghdl --std=08 --no-formal fadd8

# So some minimal optimization.
opt -full -purge
wreduce
opt -full -purge

# Save to pre processed Verilog.
write_verilog fadd8.v
